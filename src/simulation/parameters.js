// Los parámetros de la simulación de agentes (Kuramoto, deambular, pulso,
// efectos por ventana, sonido, etc.) se quitaron junto con los agentes.
// Este archivo ya no se importa desde ningún lado; se deja como un stub
// vacío por si en algún momento se quiere retomar el sistema de agentes.
export function createParameters() {
  return {};
}